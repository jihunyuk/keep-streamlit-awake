name: Keep Streamlit Awake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit app
        run: |
          curl -sS -o /dev/null -A "Mozilla/5.0" https://words-app-jihunyuk.streamlit.app/
